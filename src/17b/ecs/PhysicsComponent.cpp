#include "PhysicsComponent.hpp"

void cPhysicsComponent::Update()
{
	if(!mpBody)
		return;
	
	// TODO
	//mpEntity->GetTransform().mvPosition = mpBody->GetPosition();
	//mpEntity->GetTransform().mvRotation = mpBody->GetRotation();
};